name: Public-Safe Parallel Multi-Target Katana Scan

on:
  workflow_dispatch: 

jobs:
  # [1단계] Secrets에서 URL 개수를 파악하여 가상머신 번호 배열([0, 1, 2...])을 동적으로 생성
  setup-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix-json: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: 가상머신 인덱스 생성
        id: set-matrix
        run: |
          JSON_STRING=$(echo "${{ secrets.TARGET_URLS }}" | jq -R -s -c 'split("\n") | map(select(length > 0)) | keys')
          echo "matrix=$JSON_STRING" >> $GITHUB_OUTPUT

  # [2단계] 최대 20대의 가상머신을 동시에 띄워 헤드리스 스텔스 스캔 진행
  katana-parallel-scan:
    needs: setup-matrix
    runs-on: ubuntu-latest
    strategy:
      matrix:
        index: ${{ fromJson(needs.setup-matrix.outputs.matrix-json) }}
      max-parallel: 20
      fail-fast: false
    
    steps:
      - name: 타겟 URL 추출 및 마스킹 (보안 핵심)
        run: |
          INDEX=${{ matrix.index }}
          LINE_NUM=$((INDEX + 1))
          TARGET_URL=$(echo "${{ secrets.TARGET_URLS }}" | sed -n "${LINE_NUM}p" | tr -d '\r' | xargs)
          echo "::add-mask::$TARGET_URL"
          echo "TARGET_URL=$TARGET_URL" >> $GITHUB_ENV

      - name: 도구 및 라이브러리 설치
        run: |
          go install github.com/projectdiscovery/katana/cmd/katana@latest
          
      - name: Katana 개별 헤드리스 스캔 & 실시간 로그 모니터링
        run: |
          export PATH=$PATH:$(go env GOPATH)/bin
          HOSTNAME=$(echo "$TARGET_URL" | sed -e 's|^[^/]*//||' -e 's|/.*||' -e 's|:.*||')
          echo "🌐 스캔 대상 호스트 제한 정규식: $HOSTNAME"
          
          export ROD_FLAGS="--no-sandbox --disable-setuid-sandbox"
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 || true
          
          # Katana 실행
          katana -u "$TARGET_URL" \
            -c 1 \
            -rl 3 \
            -rd 2 \
            -d 2 \
            -cs "$HOSTNAME" \
            -sc \
            -headless \
            -v \
            -jc \
            -jsonl \
            -o katana_output.jsonl > katana_verbose.log 2>&1 &
          
          KATANA_PID=$!
          echo "⏳ [VM-${{ matrix.index }}] 샌드박스 우회형 정밀 헤드리스 스캔 시작 (PID: $KATANA_PID)"
          
          while kill -0 $KATANA_PID 2>/dev/null; do
            sleep 30
            if [ -f katana_output.jsonl ]; then
              CURRENT_COUNT=$(wc -l < katana_output.jsonl)
              echo "📊 [VM-${{ matrix.index }} 진행 상황] 현재까지 지정 호스트 내부에서 ${CURRENT_COUNT}개의 엔드포인트 수집..."
            else
              echo "📊 [VM-${{ matrix.index }} 진행 상황] 브라우저 시동 및 렌더링 준비 중..."
            fi
          done
          
          wait $KATANA_PID
          EXIT_CODE=$?
          
          if [ -f katana_output.jsonl ]; then
            FINAL_COUNT=$(wc -l < katana_output.jsonl)
          else
            FINAL_COUNT=0
            touch katana_output.jsonl
          fi
          
          if [ $FINAL_COUNT -eq 0 ] || [ $EXIT_CODE -ne 0 ]; then
            echo "🚨 [VM-${{ matrix.index }} 원인 분석] 수집 결과가 없거나 에러가 났습니다. 아래 상세 로그를 확인하세요:"
            echo "----------------------------------------------------"
            cat katana_verbose.log
            echo "----------------------------------------------------"
          else
            echo "🏁 [VM-${{ matrix.index }} 완료] 스캔 성공! (최종 수집: ${FINAL_COUNT}개)"
          fi

      - name: 결과물 비밀번호 압축 암호화 (★특수문자 차단 우회 보호)
        env:
          ZIP_PWD: ${{ secrets.ARTIFACT_PASSWORD }}
        run: |
          # 비밀번호를 환경변수 $ZIP_PWD로 안전하게 넘겨 깨짐 현상을 원천 차단합니다.
          zip -P "$ZIP_PWD" -r "katana_output_${{ matrix.index }}.zip" katana_output.jsonl

      - name: 임시 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: part-${{ matrix.index }}
          path: katana_output_${{ matrix.index }}.zip
          retention-days: 1

  # [3단계] 모든 가상머신의 스캔이 끝나면 결과들을 수집해 암호를 풀고 엑셀로 병합
  aggregate-results:
    needs: [setup-matrix, katana-parallel-scan]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: 레포지토리 코드 가져오기
        uses: actions/checkout@v4

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 필수 라이브러리 설치
        run: pip install pandas openpyxl

      - name: 모든 가상머신의 암호화 임시 파일 다운로드
        uses: actions/download-artifact@v4
        with:
          pattern: part-*
          path: encrypted_parts
          ignore-on-missing-artifact: true

      - name: 비밀번호 해독 및 압축 해제 (★안정성 로그 대폭 강화)
        env:
          ZIP_PWD: ${{ secrets.ARTIFACT_PASSWORD }}
        run: |
          mkdir -p katana_outputs
          if [ -d encrypted_parts ]; then
            find encrypted_parts -name "*.zip" | while read -r zipfile; do
              echo "📦 [해독 시작] 파일 분석 중: $zipfile"
              
              # 환경변수를 사용해 특수문자가 섞인 암호도 완벽하게 해독합니다.
              unzip -P "$ZIP_PWD" -o "$zipfile" -d temp_unzip
              
              if [ -f temp_unzip/katana_output.jsonl ]; then
                idx=$(basename "$zipfile" .zip | sed 's/katana_output_//')
                mv temp_unzip/katana_output.jsonl "katana_outputs/part_${idx}.jsonl"
                echo "🟢 [해독 성공] -> katana_outputs/part_${idx}.jsonl 매핑 완료"
              else
                echo "❌ [해독 실패] 비밀번호가 틀렸거나 압축 파일이 손상되었습니다: $zipfile"
              fi
              rm -rf temp_unzip
            done
          fi

      - name: 파이썬 스크립트로 단일 엑셀 변환
        run: python json_to_excel.py

      - name: 최종 통합 엑셀 파일 Discord로 안전 전송 (GitHub에 남기지 않음)
        run: |
          if [ -s katana_multi_scan_report.xlsx ]; then
            curl -H "Content-Type: multipart/form-data" \
                 -F "file=@katana_multi_scan_report.xlsx" \
                 -F "payload_json={\"content\": \"🚀 **가상 환경 제약을 해결한 통합 병렬 Katana 헤드리스 스캔 완료!** 다른 서브도메인을 제외한 최종 통합 엑셀 파일입니다.\"}" \
                 "${{ secrets.DISCORD_WEBHOOK }}"
          else
            curl -H "Content-Type: application/json" \
                 -X POST \
                 -d "{\"content\": \"⚠️ 병렬 스캔 완료되었으나 모든 타겟이 차단되었거나 수집된 엔드포인트가 없습니다.\"}" \
                 "${{ secrets.DISCORD_WEBHOOK }}"
          fi
